{
    "build": {
      "builder": "dockerfile"
    },
    "deploy": {
      "startCommand": "sh -c \"php artisan migrate --force && php artisan config:clear && php artisan view:clear && php -S 0.0.0.0:${PORT} -t public\"",
      "restartPolicyMaxRetries": 5
    }

}
